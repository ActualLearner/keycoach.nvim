# Keep observation and analysis local and content-free

The product records broad editor activity but stores only normalized Observations on the user's machine, never inserted text, search or command arguments, clipboard data, file paths, or project names. Cloud analysis could infer more intent, but local content-free analysis is required to make passive observation trustworthy and to keep the product free to operate.
