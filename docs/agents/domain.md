# Domain Docs

Before exploring, read `CONTEXT.md` and relevant ADRs under `docs/adr/`.
Proceed silently when they do not exist; domain-modeling creates them lazily.

This is a single-context repository:

```
/
|-- CONTEXT.md
|-- docs/adr/
`-- src/
```

Use terminology defined in `CONTEXT.md`. Surface conflicts with existing ADRs
instead of silently overriding them.
